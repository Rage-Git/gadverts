# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea/

# brand source assets are kept in-repo intentionally (logos, exports);
# but ignore heavy raw drops that shouldn't ship:
/brand/_raw/

# original raw logo drops at repo root — clean copies live in public/brand/.
# Kept on disk locally, not tracked (avoid duplication in the repo).
/g adverts logo*.jpeg
/g adverts logo*.png
